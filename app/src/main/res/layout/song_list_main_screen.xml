<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:id="@+id/listItemContainer"
  android:layout_width="match_parent"
  android:layout_height="@dimen/song_list_height">

  <ImageView
    android:id="@+id/songImage"
    android:layout_width="@dimen/song_list_height"
    android:layout_height="@dimen/song_list_height"
    android:layout_marginStart="30dp"
    android:contentDescription="@string/song_content_description_image"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    tools:srcCompat="@drawable/avicii_background" />

  <TextView
    android:id="@+id/name"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/padding_half"
    android:layout_marginEnd="@dimen/padding_standard"
    android:layout_marginStart="@dimen/padding_standard"
    android:layout_marginTop="@dimen/padding_half"
    android:textColor="@android:color/black"
    android:textSize="@dimen/song_name_text_size"
    android:textStyle="bold"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toEndOf="@+id/songImage"
    app:layout_constraintTop_toTopOf="parent"
    tools:text="Food Name" />

  <ImageView
      android:id="@+id/favoriteButton"
      android:layout_width="@dimen/marios_button_size"
      android:layout_height="@dimen/marios_button_size"
      android:layout_marginEnd="@dimen/padding_standard"
      android:contentDescription="@string/its_me_a_button"
      android:scaleType="center"
      android:src="@drawable/ic_favorite_border"
      app:layout_constraintBottom_toBottomOf="parent"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintTop_toTopOf="parent" />

</android.support.constraint.ConstraintLayout>